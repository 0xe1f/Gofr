Mask the continue marker (currently sent in base-64)
Localization
Use separate queues for various tasks
Use a mutex to prevent simultaneous feed updates
Unread count can't be relied on to always be correct. A separate cron job should routinely correct discrepancies
