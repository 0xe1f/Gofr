Add subscription refreshes
Mask the continue marker (currently sent in base-64)
Localization
Tasks will currently re-run when failed. Disable this for user-initiated tasks.
Use separate queues for various tasks
